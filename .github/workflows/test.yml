name: Tests

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: |
            package-lock.json
            frontend/klask-4/package-lock.json

      - name: Install root dependencies
        run: npm ci

      - name: Install Klask-4 frontend dependencies
        run: npm --prefix frontend/klask-4 ci

      - name: Build Klask-4 frontend
        run: npm --prefix frontend/klask-4 run build

      - name: Run logic tests with coverage
        run: npm run test:coverage
